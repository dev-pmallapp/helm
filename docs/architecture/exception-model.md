# Exception Model

AArch64 exception handling in HELM — levels, routing, delivery, and
return.

## Exception Levels

HELM models all four AArch64 exception levels:

| EL | Purpose | Used In |
|----|---------|---------|
| EL0 | User-space | SE mode (default), FS mode apps |
| EL1 | OS kernel | FS mode Linux |
| EL2 | Hypervisor | FS mode (planned) |
| EL3 | Secure monitor | FS mode (planned) |

`Aarch64Regs` carries per-EL state: SP_ELx, ELR_ELx, SPSR_ELx,
VBAR_ELx, SCTLR_ELx, TCR_ELx, TTBR0/1_ELx, ESR_ELx, FAR_ELx.

## Synchronous Exceptions

Generated by instruction execution:

- **SVC** — syscall from EL0 to EL1 (or emulated in SE mode).
- **HVC** — hypervisor call from EL1 to EL2.
- **SMC** — secure monitor call to EL3.
- **Undefined instruction** — unimplemented opcode.
- **Data abort** — translation fault, permission fault, alignment fault.
- **Instruction abort** — translation fault on fetch.

## Asynchronous Exceptions (Interrupts)

- **IRQ** — routed through the GIC to the CPU via `IrqSignal`.
- **FIQ** — fast interrupt (used for secure interrupts).
- **SError** — system error (external abort).

The CPU checks `IrqSignal::is_raised()` at the top of `step()` or at
block boundaries in the TCG loop. If DAIF.I is clear, it takes an IRQ
exception.

## Exception Delivery

When an exception is taken (implemented in `Aarch64Cpu`):

1. Save current PSTATE → SPSR_ELx.
2. Save current PC → ELR_ELx.
3. Write ESR_ELx with exception class and ISS.
4. For data/instruction aborts, write FAR_ELx with the faulting VA.
5. Mask DAIF bits (disable interrupts).
6. Set SP to SP_ELx (based on SPSel).
7. Branch to VBAR_ELx + vector offset:
   - `0x000` — synchronous, current EL, SP_EL0
   - `0x200` — synchronous, current EL, SP_ELx
   - `0x400` — synchronous, lower EL, AArch64
   - `0x480` — IRQ, lower EL, AArch64
   - `0x500` — FIQ, lower EL, AArch64
   - `0x580` — SError, lower EL, AArch64

## Exception Return (ERET)

The `ERET` instruction:

1. Restores PSTATE from SPSR_ELx (including EL, SP, DAIF, NZCV).
2. Branches to ELR_ELx.
3. Updates `current_el`, `sp_sel`, and DAIF from the restored PSTATE.

## SE Mode Handling

In SE mode, SVC does not take a real exception. Instead,
`Aarch64Cpu::step()` returns `HelmError::Syscall { number, .. }` and
the SE runner dispatches to `Aarch64SyscallHandler::handle()`.
