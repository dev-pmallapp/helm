[package]
name    = "helm-diag"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[features]
default      = []
log-fallback = ["dep:log"]

[dependencies]
log = { version = "0.4", optional = true }
