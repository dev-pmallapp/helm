"""HELM simulation configuration scripts.

Directory layout mirrors gem5::

    configs/
      common/          # Shared helpers (cpu, cache, options, simulation)
      components/      # Reusable board & SoC definitions
      example/
        se/            # Syscall-emulation configs
        fs/            # Full-system configs (future)
          arm/         # ARM board definitions
"""
